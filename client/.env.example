# Keycloak Configuration
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=your-realm
VITE_KEYCLOAK_CLIENT_ID=your-client-id

# API Configuration
# Default: use Vite dev proxy by keeping base URL relative ('/api') in code
# Configure the proxy target here for dev:
VITE_API_TARGET=http://localhost:8081

# Optional: If you need to bypass the proxy and point directly, uncomment below.
# VITE_API_BASE_URL=http://localhost:8081/api

# Development Settings
VITE_DEBUG_MODE=true
